export type PreviewFilm = {
  id: string;
  title: string;
  image: string;
  weekend: string;
  gross: string;
};

export type DetailFilm = {
  id: string;
  title: string;
  image: string;
  plot: string;
};
